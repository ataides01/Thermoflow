# Thermoflow MVP

MVP em Django para apresentacao comercial da Thermoflow, inicialmente sem banco de dados.

## Como rodar

```bash
pip install -r requirements.txt
python manage.py runserver
```

Depois acesse `http://127.0.0.1:8000/`.
